import UIKit

/// Placeholder view controller for the Progress Statistics screen.
/// Replace the body with your actual analytics / learning-progress UI.
class ProgressStatisticsViewController: UIViewController {

    override func viewDidLoad() {
        super.viewDidLoad()
        title = "Progress Statistics"
        view.backgroundColor = .systemBackground
    }
}
